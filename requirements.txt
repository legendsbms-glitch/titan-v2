# TITAN v2.0 — Python Dependencies (all free)
# Install: pip install -r requirements.txt

# ── Core Data ──────────────────────────────────────
yfinance>=1.3.0
pandas>=2.0.0
numpy>=1.24.0

# ── Technical Analysis ─────────────────────────────
ta>=0.11.0                    # Technical indicators (pandas-ta alternative for Python 3.11)

# ── Statistics & ML ───────────────────────────────
scipy>=1.11.0
scikit-learn>=1.3.0
hmmlearn>=0.3.0               # Hidden Markov Models (regime detection)
arch>=6.2.0                   # GARCH volatility models

# ── NLP & Sentiment ────────────────────────────────
feedparser>=6.0.10             # RSS feed parsing
requests>=2.31.0

# ── API & Serving ──────────────────────────────────
fastapi>=0.104.0
uvicorn>=0.24.0
httpx>=0.25.0

# ── Dashboard ──────────────────────────────────────
streamlit>=1.28.0
plotly>=5.18.0

# ── Database ───────────────────────────────────────
sqlalchemy>=2.0.0

# ── Scheduling ─────────────────────────────────────
APScheduler>=3.10.4

# ── Utilities ──────────────────────────────────────
python-dotenv>=1.0.0

# ── Optional: Telegram Alerts ─────────────────────
# Already covered by requests

# ── Optional: FinBERT (heavy, ~500MB download) ─────
# Uncomment if you want ML-based sentiment:
# transformers>=4.35.0
# torch>=2.0.0

# ── Optional: Bayesian inference ──────────────────
# pip install pymc pgmpy

# ── Optional: Hyperparameter optimization ─────────
# pip install optuna

# ── Testing ────────────────────────────────────────
pytest>=7.0.0
